# Templates

This MVP uses simple visual heuristics for occupancy detection. Put future board,
bench, or item template images in this folder if you replace the heuristic in
`tft_analyzer/vision/occupancy.py` with OpenCV template matching.
